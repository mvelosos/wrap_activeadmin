class Create<%= class_name.pluralize %> < ActiveRecord::Migration[5.0]
  def change
    create_table :<%= class_name.underscore.pluralize %> do |t|
      t.string :title
      t.text :body
      t.references :<%= class_name.underscore %>_category
      t.string :slug
      t.boolean :featured
      t.boolean :published, default: false
      t.datetime :published_date
      t.string :image
      t.timestamps
    end
  end
end
