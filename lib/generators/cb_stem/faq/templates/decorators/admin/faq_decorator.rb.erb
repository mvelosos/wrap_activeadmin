# Decorates <%= class_name %> for ActiveAdmin
class Admin::<%= class_name %>Decorator < ApplicationDecorator

  decorates :<%= class_name.underscore %>

  def question_with_link
    link_to truncate(question, length: 20), [:edit, :admin, object]
  end

  def answer
    object.answer&.html_safe
  end

end
